# Dependencies
node_modules/

# Build output
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Local env / secrets
.env
.env.local
.env.*.local

# This project intentionally has no lockfile — Vercel runs `npm install`.
package-lock.json

# macOS
.DS_Store

# Local Claude Code preview config (machine-specific paths)
.claude/launch.json
worker/.env

# Countryman Stables recovery working files (archive downloads, logo work)
cms-recovery/
