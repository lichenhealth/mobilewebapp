# Lichen — PWA project guide

Lichen is a holistic-healing nonprofit + conscious-economy platform. This repo
(`lichenhealth/mobilewebapp`, package `lichen-pwa`) is the member-facing PWA.
The founder is non-technical; explain reasoning briefly and prefer small,
reviewable changes. Keep Plan Mode on for anything touching the database or auth.

## Stack & deploy
- React 18 + Vite + TypeScript. Build: `tsc -b && vite build` (it type-checks — always run before considering a change done).
- No package-lock; Vercel runs `npm install`. Vercel auto-deploys `main` on push.
- Backend: Supabase (project `mjqnaevertyzgjlpwynr`). Anon key is hardcoded in `src/lib/supabase.ts` (public by design; RLS protects data).
- DB schema is version-controlled in `supabase/migrations/00000000000000_init.sql` — a `pg_dump --schema-only --schema=public` baseline of the live DB (captured 2026-06-29) plus a manually-appended `auth.users` signup trigger. It's a record/backup, not auto-applied. The live schema is still edited in the Supabase dashboard, so this file can drift — refresh it after DB changes. Tooling: Supabase CLI is linked (`supabase/config.toml`); `pg_dump` lives at `/opt/homebrew/opt/libpq/bin/pg_dump` (no Docker). `supabase db dump`/`db pull` need Docker, which isn't installed — use the direct `pg_dump` against the session pooler instead.
- Marketing site is a separate repo (`lichenhealth/lichen-health`, Astro) — not this one.
- Two domains: **`lichen.healthcare`** = this PWA (DNS on Vercel) · **`lichen.health`** = marketing site (website served by Vercel, but DNS nameservers are still on **Wix** — only the A record was moved, not the nameservers). Transactional email sends from `lichen.healthcare` because Wix can't host the subdomain MX Resend needs. Eventual consolidation onto one domain (for shared login) is an open strategy question.
- Run `tsc -b` and `vite build` locally before committing; fix all type errors (the project builds clean today).

## Conventions
- Design tokens live in `src/styles/tokens.css` (peach accent `--peach`, bone canvas, Archivo body, display serif). Reuse token vars; match existing BEM-ish class names (`.onb__`, `.prof__`, `.cat__`, `.adminc__`, `.conv-row`, `.thread__`).
- Logo: `src/components/LichenMark.tsx`. Icons: `src/components/Icon.tsx` (`<Icon name=... size=... />`).
- Avoid the word "spaces" in member-facing UI — say organizations / communities / groups / places.
- Supabase embeds: when a table has >1 FK to another, disambiguate with the explicit hint, e.g. `profiles!subscriptions_profile_id_fkey(full_name)`.
- New enum values can't be USED in the same migration that adds them — add in a separate step that's committed first.
- Only 3 columns use real Postgres enums (`account_capability`, `space_kind`, `space_member_role`). The other "enum-like" fields below (care `status`, subscription `tier`/`source`, category `domain`, suggestion `status`) are actually `text` columns with CHECK constraints — to add a value, change the CHECK, not an enum type.

## Data model (all RLS-enabled)
- `profiles` (id→auth.users; full_name, headline, bio, email, onboarded, is_admin). Trigger creates a profile on signup.
- `profile_capabilities` (service_provider / goods_provider).
- `categories` (63 seeded; text slug id, domain good|service, name, sort) + `profile_categories`. Member-suggested categories via `category_suggestions` + admin approval RPCs `approve_category_suggestion` / `reject_category_suggestion`.
- `spaces` (kind enum: organization|community|group|place; created_by) + `space_members` (role enum: super_admin|admin|member). Creator becomes super_admin via trigger `handle_new_space`. One super_admin per space.
- `chats` (one per space — all kinds — and one per care team) + `chat_members` (synced to space_members via trigger) + `chat_messages` (realtime enabled). RLS uses `is_chat_member()` helper to avoid recursion.
- `care_team_members` (patient_id, caregiver_id, status pending|active, initiated_by). Either party initiates; the OTHER approves. Activation trigger creates/join the care-team chat.
- `care_invitations` (invite non-members by email; `claim_care_invitations()` RPC converts them to pending care links on signup).
- `subscriptions` (tier community|concierge, source gift|stripe). Admin RPCs `gift_subscription` / `revoke_subscription`. Stripe columns exist but Stripe is NOT wired yet.

## Admin
- `profiles.is_admin = true` gates the admin screens (Review categories `/admin/categories`, Gift access `/admin/supporters`). Admin links live in `SideMenu` in `.side-menu__admin` so they show on mobile too (the primary block is hidden on mobile).

## Key screens
- `Onboarding.tsx` — four space-kind sections + provider category pickers (`CategoryPicker.tsx`, searchable, fuzzy-match suggest-new flow).
- `Profile.tsx` — about, capabilities + category editing, per-kind space sections, care-team management (invite/approve/remove, non-member email invites), tier badge.
- `Chat.tsx` / `ChatThread.tsx` — real Supabase chats + messages + realtime (`src/lib/chatApi.ts`), incl. 1:1 DMs (`ensure_direct_chat`), private media (chat-media bucket, signed URLs), reactions & replies. Signal-style: inbox = communities/groups/DMs; care-team chat lives in Concierge; event rooms live on event pages; help rooms show in the SUPPORT ACCOUNT's inbox only. chats.kind now spans organization|community|group|place|care_team|direct|event|help.
- `Help.tsx` (`/help`, side-menu "Help") — SHIPPED 2026-07-05 (PR #41): every member gets a private support room with the Lichen help account (connect@lichen.health, is_admin) via `ensure_help_chat()`; member sees "Lichen", support sees the member's name in the normal Chat inbox. Same PR: branded auth email templates (`supabase/templates/`) + Resend custom SMTP (configured in dashboard 2026-07-05 — auth emails send from support@lichen.healthcare).
- `Concierge.tsx` — REAL data: WOW/KOC are a per-patient `care_posts` board (`src/lib/conciergeApi.ts`; posts carry text/media/links/link-previews, WOW 0–100 scores → derived `HexagonRadar`, KOC scheduled via the shared recurrence engine). Caregiver dashboard at `/caregiver`; patient-scoped client view. Urgent tab is REAL (2026-07-05): on-call roster from `on_call_roster(patient)` RPC (active caregivers + their `availability_windows.kind='on_call'` windows + phone; SECURITY DEFINER, gated to the patient or a fellow active caregiver — the only mock left in Concierge is gone). Call = `tel:`, Text = DM via `ensure_direct_chat` with the context message as first message.
- `Events.tsx` / `EventPage.tsx` — REAL (2026-07-04): Events feed (category tabs Social/Work-Charity/Learning/Experiences, Free/Trade/Paid circles, date-badge cards with RSVP/Book) + full event pages (About/Updates/Chat/RSVP/Book tabs). EVENT CHAT is live (2026-07-05, PR #39): chats.kind 'event', room = host + going + TENTATIVE (`ensure_event_chat()` + `sync_attendee_to_event_chat` trigger; cancel/decline → out, host stays; rooms stay out of the Chat inbox). RSVP has three answers — Going / Maybe (status 'tentative') / Can't make it — on the event page AND the calendar invite sheet; maybes count as busy, show `~`/peach-outline, and host notifications say "might come". An event post = posts row (content_type 'actionable', service_areas ⊇ ['events'], event_category/event_mode) LINKED to a real calendar event (posts.linked_event_id); RSVP = event_attendees upsert (self policy) → lands guests' calendars + notifies host; guest lists + linked events inherit POST visibility via RLS subqueries. ⚠ RLS recursion gotcha (fixed 2026-07-05, PR #43): policies must NOT form cycles — events' guest check goes through SECURITY DEFINER `is_event_attendee()`; use that pattern whenever two tables' policies reference each other. Compose has a unified offer block (events: Free/Trade/Paid+sliding; marketplace-only adds Lend/Rent; one Location; When = events only). Link previews (details.previews via link-preview edge fn) render on ALL feed cards + event pages. 'rsvp' envelope icon = the Events mark everywhere. NAV (2026-07-05, PR #44, Figma 286-11251): top tabs Events | My Events (/events/mine — hosting+going+maybe, upcoming + collapsed past; feed-derived, 50-post cap) | My Calendar (→ /calendar); categories are sub-filters. HOST EDITING (PR #45): Edit/Cancel row on own event pages; Compose `?post=<id>` edit mode (full prefill incl. sliding-scale round-trip; 'events' area locked while a calendar event is linked); event fields save via `updateEventDetails()` — NEVER `updateEvent()` from Compose, its invitee sync wipes RSVPs. Cancel deletes event first (cascades calendars + chat room) then post. Posts have author update/delete policies since `20260712120000_posts_edit.sql`.
- `Calendar.tsx` — REAL (built 2026-07-03/04, "everything has a calendar"): timed/all-day/multi-day/recurring events (`src/lib/calendarApi.ts`, shared `src/lib/recurrence.ts` RRULE-lite engine + `occursOn`), invites/RSVP with notifications, Day/3-Days/Week/Month views, search (events + calendars-as-overlays), drag-to-create, pinned toolbar/day-header (NOTE: `.app-shell` must stay `overflow-x: clip`, never `hidden` — hidden kills position:sticky app-wide). Availability hours + audience visibility rules (`calendar_shares`, most-specific-wins, `free_busy()`/`availability_of()` RPCs) + group find-a-time overlay + settings at `/calendar/settings`. Composer (`EventComposer.tsx`) has a live invite preview (desktop) + smart locations (Zoom/Meet → "Join", address → Google Maps link-out).
- Edge functions (Resend; deploy via `supabase functions deploy <name>`, no Docker needed; both read the `RESEND_API_KEY` secret):
  - `send-care-invite` — care-team invites.
  - `send-invite` — general "Invite to Lichen" member invites. Powers the `/invite` screen (`Invite.tsx`, side-menu entry "Invite to Lichen"); emails a `/signup` link, no tracking/DB. The screen guards against inviting existing members via the `find_member_by_email()` RPC.

## Secrets — never hardcode
RESEND_API_KEY, any Stripe keys, Supabase service-role key live in Supabase secrets / env, never in the repo.

## Real-data architecture (design locked 2026-06-30 — the next major build)
Turning the mock feed/marketplace/mycelium into a real platform. Cornerstone decisions:
- **Members are ENTITIES, not just people.** An entity has a `kind` (person / plant / animal / place / organization). Humans log in; non-humans (e.g. *Tango* the therapy horse, a *San Pedro* cactus) are **stewarded** by a human and act through them. Contributions and value accrue to the *entity itself* (the conscious-economy ledger — a cross-species economy). Today `profiles` = humans (auth-linked). Generalizing `profiles` into multi-species entities (add `kind` + `steward_id`, allow non-auth profiles) is a foundational step; `posts`/`mycelium` are designed to absorb it (an author stays a profile row that *becomes* an entity).
- **Posts = one unified content stream.** Fields: author, optional `space_id`, `visibility` (public/mycelium/space), `content_type` (social/creative/educational/actionable/qa), optional `service_area` (marketplace/work/courses/food/art/events/places/library/people), title/body/image, `details` jsonb (area-specifics: price, schedule, location…).
- **Mycelium = your trusted web.** "Trust" and "Add to Mycelium" are the SAME action — a polymorphic edge `you → (target_type, target_id)` over profiles/spaces/posts (eventually any entity). Trusting an entity flows its posts/marketplace items into *your* mycelium feed/marketplace. **Recommend** = amplify a specific post to the people who trust you.
- **Mycelium is a trust LENS over the WHOLE platform.** Everywhere (any community/space), an item's trust/recommend icons highlight (peach/orange) iff someone **in YOUR mycelium** (not that space) trusted/recommended it; hover shows *which of your mycelium members*. Always "what my mycelium endorses," never the broader platform. Quality over quantity. → the endorsement overlay is a query: `(endorsers of item) ∩ (your mycelium)`.
- **Home feed = public ∪ your spaces ∪ content from entities in your mycelium**, narrowed by the content-type chips + service-area lenses. Service-area lenses are **contextual** (in Mycelium they scope to your web; in a space, to that space; on Home, platform-wide).
- **Membership gate**: enforce (active community+ subscription, gift OR paid) at the **RLS level as real data lands** — NOT before (mock content isn't worth gating, and it'd lock out beta). The frontend routing-gate flips LAST. Admins always bypass.

Build order: `posts`/`mycelium`/`recommendations` tables → wire Home feed to real posts → Trust/Recommend + Add-to-Mycelium actions + endorsement overlays → marketplace & other lenses → entity generalization (multi-species + stewardship) → value ledger → membership gate.

## Open threads / next up
1. ~~Stripe checkout + webhook~~ — DONE / LIVE (2026-06-30). Self-serve Community ($29)/Concierge ($99, bundle incl. community) memberships via Stripe. Edge functions `stripe-checkout` (Checkout session), `stripe-webhook` (records tier/status into `subscriptions`, `source='stripe'`; `verify_jwt=false`), `stripe-portal` (upgrade/downgrade/cancel via Billing Portal). `/membership` screen + side-menu entry. IMPORTANT: these call the Stripe REST API via **raw `fetch`** (the SDK's HTTP client fails in Deno edge); secrets are read with `.replace(/[^\x21-\x7E]/g,'')` to survive paste artifacts. Live product IDs are hardcoded (`prod_SHphERWwRzXVak` community / `prod_SHpjCV2Jcfkeuo` concierge). Single Supabase project = test and live share secrets, so the project is now in LIVE Stripe (no test sandbox). Still open: (a) gift→paid conversion, (b) reconcile marketing-site Payment Link subs (not captured by our webhook) with app subs, (c) enforce the membership gate (paying doesn't yet restrict access).
2. ~~Resend email~~ — DONE (2026-06-29). `send-care-invite` edge function is written, deployed, and verified sending real invites. Sends from `Lichen <care@lichen.healthcare>` (NOT lichen.health — see domains note below). `RESEND_API_KEY` is set as a Supabase Edge Function secret. Resend domain `lichen.healthcare` is verified (DKIM/SPF/MX added to Vercel DNS via auto-config). Deploy with `supabase functions deploy send-care-invite` (works without Docker — ignore the "Docker is not running" warning).
3. ~~Concierge WOW/KOC to real data~~ — DONE (2026-07-02/03, care_posts board). ~~Urgent tab on-call roster~~ — DONE (2026-07-05, PR #42, migration `20260711120000_concierge_oncall.sql`). Same PR: profiles gained `first_name`/`last_name` (granted; trigger `compose_full_name` keeps `full_name` in sync — never write full_name client-side) and `phone` (deliberately NOT in the column-scoped SELECT grant; owner reads via `my_phone()`, care team via `on_call_roster()` — email privacy pattern). Onboarding now REQUIRES first/last/phone (both finish and "later" gate on them).
4. Advanced search panel (Figma mockup exists).
8. Urgent-care SMS notify (founder idea 2026-07-05): "Send to X" should ALSO text the on-call caregiver's phone server-side (Twilio or similar — needs provider account, per-message cost, US A2P registration). Today: Call/Text buttons open the patient's own dialer/SMS composer (`tel:`/`sms:` links, message prefilled); in-app Send goes to the DM only.
6. ~~Events feature~~ — DONE / LIVE (2026-07-04, see Events.tsx above). ~~(a) EVENT CHAT~~ — DONE / LIVE (2026-07-05, PR #39, incl. RSVP "Maybe"). Still open: (b) event invites to Groups/Places (composer copy promises it; search still member-only); (c) previews/thumbnails for CALENDAR-only invites (events table has no previews storage).
7. Calendar deferred (agreed 2026-07-03/04): (a) external calendar import — Google/Apple/Outlook secret iCal URL → busy blocks feed free_busy (plan "part 4"); (b) resources — space-owned rooms/tools with bookable calendars (plan "part 5"; `events_one_owner` CHECK widens to `owner_resource_id`); (c) address autocomplete + Lichen-place locations → wait for Maps; (d) touch drag-to-create (mouse-only today).
5. ~~Privacy hardening: tighten `profiles` email visibility~~ — DONE (2026-06-29). `profiles` SELECT for `authenticated` is now column-scoped to every column EXCEPT `email`. Own email comes from the auth session; care-invite lookup uses `find_member_by_email()` and the admin supporter list uses `admin_list_supporters()` (both SECURITY DEFINER). Migrations `20260629120000_add_member_email_rpcs.sql` + `20260629120100_restrict_profile_email.sql`. Still open: broader privacy review of other tables before public launch.

## Housekeeping
- Downloads folder has accumulated many `lichen-*` zip duplicates from the old manual deploy flow — irrelevant to the repo, but the source-of-truth is always `main` on GitHub.
- There may be stray leftover files under `src/data/` (old zips, nested dirs) from earlier bad copies — safe to clean up if found.
